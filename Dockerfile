FROM node

# lets define the WORKDIR

# now we should COPY the src and test directories, as well as package.json .babelrc .service

# and then RUN npm to install, test and build our server

# EXPOSE the correct port

CMD npm start
